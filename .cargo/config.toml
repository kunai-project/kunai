[build]
# make final binary statically linked against libc
rustflags = ["-Ctarget-feature=+crt-static"]

[alias]
xtask = "run -q --package xstub --release --"
xrun = "xtask run"
xbuild = "xtask build"
xrelease = "xtask release"
